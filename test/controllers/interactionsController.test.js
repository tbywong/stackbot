const expect = require('chai').expect;
const InteractionsController = require('../../src/controllers/interactionsController');

describe('InteractionsController', () => {
  it('should exist', () => {
    expect(InteractionsController).to.exist;
  });
});
