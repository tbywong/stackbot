const expect = require('chai').expect;
const EventsController = require('../../src/controllers/eventsController');

describe('EventsController', () => {
  it('should exist', () => {
    expect(EventsController).to.exist;
  });
});
